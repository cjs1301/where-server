spring:
  profiles:
    group:
      "local": "local"
      "prod": "prod"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    prometheus:
      enabled: true
    metrics:
      enabled: true
